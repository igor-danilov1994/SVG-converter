import * as React from 'react';
import ReactDOM from 'react-dom';
import { View } from './features/settings/template/View';

ReactDOM.render(
  <React.StrictMode>
    <View />
  </React.StrictMode>,
  document.getElementById('root'),
);
